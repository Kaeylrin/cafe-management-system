# CAFE NOWA - SYSTEM ARCHITECTURE

## 🏗️ System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        FRONTEND LAYER                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────┐              ┌──────────────────┐         │
│  │   Admin Portal   │              │   User Portal    │         │
│  │ admin-login.html │              │   login.html     │         │
│  └────────┬─────────┘              └────────┬─────────┘         │
│           │                                  │                   │
│           ├──────────────┬───────────────────┤                   │
│           │              │                   │                   │
│  ┌────────▼────────┐ ┌──▼─────────────┐ ┌──▼────────────┐      │
│  │ Admin Dashboard │ │ Employee Dash  │ │Customer Land  │      │
│  │  dashboard.php  │ │ dashboard.php  │ │  landing.php  │      │
│  └─────────────────┘ └────────────────┘ └───────────────┘      │
│                                                                   │
└───────────────────────────┬───────────────────────────────────┘
                            │
                            │ HTTPS/JSON
                            │
┌───────────────────────────▼───────────────────────────────────┐
│                        API LAYER                               │
├───────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │  login.php   │  │  users.php   │  │ audit-trail. │        │
│  │              │  │              │  │    php       │        │
│  │ • Auth       │  │ • CRUD       │  │ • View logs  │        │
│  │ • Validate   │  │ • Validate   │  │ • Filter     │        │
│  │ • Session    │  │ • Authorize  │  │ • Read-only  │        │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘        │
│         │                  │                  │                 │
│         └──────────────────┼──────────────────┘                 │
│                            │                                     │
│                     ┌──────▼──────┐                             │
│                     │ config.php  │                             │
│                     │ • DB conn   │                             │
│                     │ • Security  │                             │
│                     │ • Helpers   │                             │
│                     └──────┬──────┘                             │
│                            │                                     │
└────────────────────────────┼─────────────────────────────────┘
                             │
                             │ PDO with Prepared Statements
                             │
┌────────────────────────────▼─────────────────────────────────┐
│                     DATABASE LAYER (MySQL)                     │
├───────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐  │
│  │ super_admins   │  │     admins     │  │   employees    │  │
│  ├────────────────┤  ├────────────────┤  ├────────────────┤  │
│  │ • id           │  │ • id           │  │ • id           │  │
│  │ • username     │  │ • username     │  │ • username     │  │
│  │ • email        │  │ • email        │  │ • email        │  │
│  │ • password_hash│  │ • password_hash│  │ • password_hash│  │
│  │ • full_name    │  │ • full_name    │  │ • full_name    │  │
│  │ • created_at   │  │ • created_by   │  │ • position     │  │
│  │ • last_login   │  │ • created_at   │  │ • created_by   │  │
│  │ • is_active    │  │ • last_login   │  │ • created_at   │  │
│  └────────────────┘  │ • is_active    │  │ • last_login   │  │
│                      └────────────────┘  │ • is_active    │  │
│                                          └────────────────┘  │
│                                                                 │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐  │
│  │   customers    │  │  audit_trail   │  │login_attempts  │  │
│  ├────────────────┤  ├────────────────┤  ├────────────────┤  │
│  │ • id           │  │ • id           │  │ • id           │  │
│  │ • username     │  │ • user_type    │  │ • email        │  │
│  │ • email        │  │ • user_id      │  │ • ip_address   │  │
│  │ • password_hash│  │ • username     │  │ • attempt_time │  │
│  │ • full_name    │  │ • action       │  │ • success      │  │
│  │ • phone        │  │ • action_type  │  └────────────────┘  │
│  │ • created_at   │  │ • target_table │                       │
│  │ • last_login   │  │ • target_id    │                       │
│  │ • is_active    │  │ • ip_address   │                       │
│  └────────────────┘  │ • user_agent   │                       │
│                      │ • details      │                       │
│                      │ • timestamp    │                       │
│                      └────────────────┘                       │
│                                                                 │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Authentication Flow Diagram

```
┌──────────┐
│  User    │
│ Enters   │
│  Login   │
│  Portal  │
└────┬─────┘
     │
     ▼
┌─────────────────────────────────────────┐
│ Which Portal?                            │
├─────────────────┬───────────────────────┤
│ Admin Portal    │    User Portal        │
│ (admin-login)   │    (login.html)       │
└────────┬────────┴────────┬──────────────┘
         │                 │
         ▼                 ▼
    ┌─────────┐       ┌─────────┐
    │ Check   │       │ Check   │
    │ super_  │       │employees│
    │ admins  │       │ AND     │
    │ AND     │       │customers│
    │ admins  │       │ tables  │
    └────┬────┘       └────┬────┘
         │                 │
         └────────┬────────┘
                  │
                  ▼
         ┌────────────────┐
         │ User Found?    │
         └───┬────────┬───┘
             │NO      │YES
             ▼        ▼
        ┌─────┐   ┌────────────────┐
        │FAIL │   │ Account Active?│
        └─────┘   └───┬────────┬───┘
                      │NO      │YES
                      ▼        ▼
                 ┌─────┐   ┌──────────────┐
                 │FAIL │   │ Check Lockout│
                 └─────┘   │ (5 attempts?)│
                           └───┬──────┬───┘
                               │YES   │NO
                               ▼      ▼
                          ┌─────┐  ┌────────────┐
                          │FAIL │  │Verify Pass-│
                          │     │  │word Hash   │
                          └─────┘  └─┬──────┬───┘
                                     │WRONG │CORRECT
                                     ▼      ▼
                                ┌─────┐  ┌────────────┐
                                │LOG  │  │ Create     │
                                │FAIL │  │ Session    │
                                └─────┘  └─────┬──────┘
                                               │
                                               ▼
                                      ┌────────────────┐
                                      │ Log Audit      │
                                      │ Trail          │
                                      └────────┬───────┘
                                               │
                                               ▼
                                      ┌────────────────┐
                                      │ Redirect to    │
                                      │ Dashboard      │
                                      └────────────────┘
```

## 👥 User Creation Flow (Super Admin)

```
┌─────────────┐
│ Super Admin │
│ Clicks      │
│"Create User"│
└──────┬──────┘
       │
       ▼
┌──────────────┐
│ Select Type  │
│ • Super Admin│
│ • Admin      │
│ • Employee   │
│ • Customer   │
└──────┬───────┘
       │
       ▼
┌──────────────┐
│ Fill Form:   │
│ • Username   │
│ • Email      │
│ • Password   │
│ • Full Name  │
│ • (Position) │
│ • (Phone)    │
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│ Frontend         │
│ Validation       │
│ • Email format   │
│ • Password length│
│ • Required fields│
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ POST to          │
│ api/users.php    │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Check Permission │
│ Super Admin can  │
│ create all types │
│ Admin can create │
│ emp/customer only│
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Validate Input   │
│ • Sanitize       │
│ • Check duplicate│
│ • Verify format  │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Hash Password    │
│ bcrypt cost=12   │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ INSERT INTO      │
│ appropriate table│
│ (prepared stmt)  │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Log to           │
│ audit_trail      │
│ • Who created    │
│ • What was made  │
│ • Timestamp      │
│ • IP address     │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Return Success   │
│ • New user ID    │
│ • Username       │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│ Refresh User List│
│ on Frontend      │
└──────────────────┘
```

## 🛡️ SQL Injection Prevention

```
┌──────────────────────────────────────────────────────────┐
│            TRADITIONAL (VULNERABLE) APPROACH              │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  $email = $_POST['email'];                               │
│  $query = "SELECT * FROM users WHERE email = '$email'";  │
│  $result = mysqli_query($conn, $query);                  │
│                                                           │
│  ❌ Attacker Input: admin' OR '1'='1                     │
│  ❌ Resulting Query:                                     │
│     SELECT * FROM users                                  │
│     WHERE email = 'admin' OR '1'='1'                     │
│  ❌ BREACH: Returns all users!                           │
│                                                           │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│            OUR SECURE APPROACH (PROTECTED)                │
├──────────────────────────────────────────────────────────┤
│                                                           │
│  $email = $_POST['email'];                               │
│  $stmt = $db->prepare(                                   │
│    "SELECT * FROM users WHERE email = ?"                 │
│  );                                                       │
│  $stmt->execute([$email]);                               │
│                                                           │
│  ✅ Attacker Input: admin' OR '1'='1                     │
│  ✅ Database sees: Literal string "admin' OR '1'='1"     │
│  ✅ Not treated as SQL code                              │
│  ✅ No user found → Login fails                          │
│  ✅ Attack logged in audit_trail                         │
│                                                           │
└──────────────────────────────────────────────────────────┘
```

## 📊 Audit Trail Logging Flow

```
┌──────────────────────────────────────────────────────┐
│               ANY SYSTEM ACTION                       │
│  (Login, Create User, Update, Delete, etc.)          │
└────────────────────┬─────────────────────────────────┘
                     │
                     ▼
            ┌────────────────┐
            │ Action Executes│
            │ (e.g., Login)  │
            └────────┬───────┘
                     │
                     ▼
            ┌─────────────────────────┐
            │ Call logAudit()         │
            │ Parameters:             │
            │ • user_type             │
            │ • user_id               │
            │ • username              │
            │ • action description    │
            │ • action_type           │
            │ • target_table          │
            │ • target_id             │
            │ • ip_address (auto)     │
            │ • user_agent (auto)     │
            │ • details (JSON)        │
            └────────┬────────────────┘
                     │
                     ▼
            ┌──────────────────────┐
            │ INSERT INTO          │
            │ audit_trail          │
            │ (prepared statement) │
            └────────┬─────────────┘
                     │
                     ▼
            ┌──────────────────────┐
            │ Entry Permanently    │
            │ Stored               │
            │ • Cannot be edited   │
            │ • Cannot be deleted  │
            │ • Timestamp locked   │
            └──────────────────────┘
```

## 🔐 Password Security Flow

```
┌─────────────────────────────────────────────────┐
│           PASSWORD STORAGE (Registration)        │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
        ┌────────────────┐
        │ User enters:   │
        │ "MyPassword123"│
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────────────────┐
        │ password_hash() with:      │
        │ • Algorithm: BCRYPT        │
        │ • Cost: 12                 │
        │ • Automatic salt           │
        └────────┬───────────────────┘
                 │
                 ▼
        ┌─────────────────────────────────────┐
        │ Generates hash:                     │
        │ $2y$12$randomsalt...hashedpassword  │
        │ • 60 characters                     │
        │ • Includes salt                     │
        │ • One-way (cannot be reversed)      │
        └────────┬────────────────────────────┘
                 │
                 ▼
        ┌────────────────┐
        │ Store in DB    │
        │ password_hash  │
        │ column         │
        └────────────────┘

┌─────────────────────────────────────────────────┐
│       PASSWORD VERIFICATION (Login)              │
└────────────────┬────────────────────────────────┘
                 │
                 ▼
        ┌────────────────┐
        │ User enters:   │
        │ "MyPassword123"│
        └────────┬───────┘
                 │
                 ▼
        ┌──────────────────────────┐
        │ Retrieve hash from DB:   │
        │ $2y$12$randomsalt...    │
        └────────┬─────────────────┘
                 │
                 ▼
        ┌──────────────────────────────┐
        │ password_verify(input, hash) │
        │ • Extracts salt from hash    │
        │ • Hashes input with same salt│
        │ • Compares in constant time  │
        │ • Prevents timing attacks    │
        └────────┬─────────────────────┘
                 │
          ┌──────┴──────┐
          ▼             ▼
    ┌─────────┐   ┌─────────┐
    │  MATCH  │   │NO MATCH │
    │ Success │   │  Fail   │
    └─────────┘   └─────────┘
```

## 🔒 Account Lockout Flow

```
┌────────────────┐
│ Login Attempt  │
└───────┬────────┘
        │
        ▼
┌───────────────────────────────┐
│ Query login_attempts table    │
│ WHERE:                         │
│ • email = input                │
│ • ip_address = current IP      │
│ • success = FALSE              │
│ • attempt_time > NOW() - 15min │
└───────┬───────────────────────┘
        │
        ▼
┌───────────────────┐
│ Count >= 5?       │
└───┬───────────┬───┘
    │YES        │NO
    ▼           ▼
┌────────┐  ┌──────────────────┐
│LOCKED  │  │ Proceed with     │
│REJECT  │  │ authentication   │
└────┬───┘  └────────┬─────────┘
     │               │
     │               ▼
     │      ┌────────────────┐
     │      │ Login fails?   │
     │      └───┬────────┬───┘
     │          │YES     │NO
     │          ▼        ▼
     │      ┌────────┐ ┌──────┐
     │      │INSERT  │ │UPDATE│
     │      │failed  │ │last  │
     │      │attempt │ │login │
     │      └────────┘ └──────┘
     │
     ▼
┌──────────────────────┐
│ Log lockout event    │
│ in audit_trail       │
└──────────────────────┘

┌────────────────────────────┐
│ Lockout Duration: 30 min   │
│ Auto-unlock when:          │
│ • 30 minutes pass          │
│ OR manually clear table    │
└────────────────────────────┘
```

## 🎯 Role-Based Access Control

```
┌──────────────────────────────────────────────────────┐
│              PERMISSION HIERARCHY                     │
├──────────────────────────────────────────────────────┤
│                                                       │
│  ┌────────────────────────────────────────────────┐  │
│  │         SUPER ADMIN (Highest Level)            │  │
│  ├────────────────────────────────────────────────┤  │
│  │ CAN:                                           │  │
│  │ ✅ Create/manage Super Admins                 │  │
│  │ ✅ Create/manage Admins                       │  │
│  │ ✅ Create/manage Employees                    │  │
│  │ ✅ Create/manage Customers                    │  │
│  │ ✅ View audit trail                           │  │
│  │ ✅ Full system access                         │  │
│  └─────────────────┬──────────────────────────────┘  │
│                    │                                  │
│  ┌─────────────────▼──────────────────────────────┐  │
│  │              ADMIN                              │  │
│  ├────────────────────────────────────────────────┤  │
│  │ CAN:                                           │  │
│  │ ✅ Create/manage Employees                    │  │
│  │ ✅ Create/manage Customers                    │  │
│  │ ✅ View audit trail                           │  │
│  │ ❌ Create Admins (only Super Admin can)       │  │
│  │ ❌ Create Super Admins                        │  │
│  └─────────────────┬──────────────────────────────┘  │
│                    │                                  │
│  ┌─────────────────▼──────────────────────────────┐  │
│  │            EMPLOYEE                             │  │
│  ├────────────────────────────────────────────────┤  │
│  │ CAN:                                           │  │
│  │ ✅ Access employee dashboard                  │  │
│  │ ✅ Manage orders (if implemented)             │  │
│  │ ❌ Create any users                           │  │
│  │ ❌ View audit trail                           │  │
│  │ ❌ Access admin functions                     │  │
│  └─────────────────┬──────────────────────────────┘  │
│                    │                                  │
│  ┌─────────────────▼──────────────────────────────┐  │
│  │            CUSTOMER                             │  │
│  ├────────────────────────────────────────────────┤  │
│  │ CAN:                                           │  │
│  │ ✅ Access customer landing                    │  │
│  │ ✅ Place orders (if implemented)              │  │
│  │ ✅ View own profile                           │  │
│  │ ❌ Create any users                           │  │
│  │ ❌ View audit trail                           │  │
│  │ ❌ Access admin/employee functions            │  │
│  └────────────────────────────────────────────────┘  │
│                                                       │
└──────────────────────────────────────────────────────┘
```

## 📈 Data Flow Example: Complete Login Process

```
1. User visits: login.html
2. Enters: email@example.com, password123
3. JavaScript submits POST to: api/login.php
4. API receives: {"email":"email@example.com","password":"password123","userType":"user"}
5. API extracts IP: 192.168.1.100
6. API checks lockout: SELECT COUNT(*) FROM login_attempts WHERE...
7. Result: 2 attempts (< 5, proceed)
8. API queries: SELECT * FROM employees WHERE email = ? [prepared statement]
9. Result: User found with hash: $2y$12$randomsalt...
10. API verifies: password_verify("password123", "$2y$12$randomsalt...")
11. Result: TRUE (passwords match)
12. API logs attempt: INSERT INTO login_attempts (email, ip, success) VALUES (?, ?, 1)
13. API updates: UPDATE employees SET last_login = NOW() WHERE id = ?
14. API creates session: $_SESSION['user_id'] = 123, $_SESSION['user_type'] = 'employee'
15. API logs audit: INSERT INTO audit_trail (user_type, action, ...) VALUES ('employee', 'login', ...)
16. API responds: {"success":true,"data":{"user_type":"employee","redirect_url":"..."}}
17. JavaScript receives response
18. JavaScript stores: sessionStorage.setItem('userType', 'employee')
19. JavaScript redirects: window.location.href = "../employee/dashboard.php"
20. Employee dashboard loads
21. PHP checks: if ($_SESSION['user_type'] !== 'employee') redirect
22. User is authenticated and viewing dashboard
```

---

This architecture ensures:
✅ **Security**: Multiple layers of protection
✅ **Scalability**: Easy to add new features
✅ **Maintainability**: Clean separation of concerns
✅ **Traceability**: Complete audit trail
✅ **Reliability**: Prepared statements and validation
✅ **Usability**: Clear user flows and feedback
